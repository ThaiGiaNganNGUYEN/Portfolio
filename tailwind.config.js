/** @type {import('tailwindcss').Config} */
export default {
    content: [
        "./index.html",
        "./src/**/*.{js,ts,jsx,tsx}",
    ],
    theme: {
        extend: {},
    },
    plugins: [
        // We will add the animation plugin here if you installed it
        // otherwise leave it empty for now to prevent errors.
    ],
}
